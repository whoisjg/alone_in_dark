use raylib::*;

use std::path::PathBuf;

// include!("../testing/to/sprites.rs");

fn main() {
    // assert_eq!(Sprites::DOWN_STAND.file_for(), "testing/src/down_stand.png");
    // assert!(PathBuf::from(Sprites::DOWN_STAND.file_for()).exists());
}
